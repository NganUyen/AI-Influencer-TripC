# Archive

This folder holds historical documentation that is no longer the canonical reference for the current repo state.

Examples:

- implementation summaries
- repo-root analysis notes
- point-in-time QA reports
- change logs
- refactor plans
- superseded video specs

Use top-level docs in `Docs/` first. Only use these files when you need project history or old decision context.
